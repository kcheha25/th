to reduce discrepancies and variability while preserving the same spectral reflectance distribution, the GAN-generated spectra are normalized to match the mean of the real spectra

for pixels where the spectral signature differs, the SAM naturally exceeds the fixed threshold, and the augmented spectrum corresponds to the real spectrum with added controlled noise.